name: Import OpenComputers MC1.12 Base

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled; upstream import is performed by the clean reset workflow.'
